import React from 'react';
import classes from './SideMenu.module.css';

const SideMenu = (props) => {
    return (
        <div className={classes.SideMenu}>SideMenu works!</div>
    );
}

export default SideMenu;