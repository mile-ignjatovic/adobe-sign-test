import React from 'react';
import classes from './Table.module.css';

const Table = (props) => {
    return (
        <div className={classes.Table}>Table works!</div>
    );
}

export default Table;